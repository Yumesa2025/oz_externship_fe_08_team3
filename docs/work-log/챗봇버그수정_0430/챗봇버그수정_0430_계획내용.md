# 챗봇버그수정*0430*계획내용

## 작업 계획

- 전체 목표: 챗봇 위젯의 FocusTrap 클릭 차단(#61), MessageList 마크다운 미렌더링(#62), 위젯 레이아웃 오버플로, 페이지 이동 시 위젯 미닫힘 버그를 수정한다.

## 작업01 — FocusTrap 클릭 차단 수정 (#61)

- [x] 완료

### 상세

- `src/components/chatbot/ChatbotWidget/ChatbotWidget.tsx` 수정
- FocusTrap의 `focusTrapOptions`에 `allowOutsideClick: true` 추가
- FAB(위젯 밖) 클릭과 내부 닫기 버튼 클릭이 모두 정상 동작하도록 수정

## 작업02 — MessageList 마크다운 렌더링 적용 (#62)

- [x] 완료

### 상세

- `src/components/chatbot/MessageList/MessageList.tsx` 수정
- `renderAssistantContent`에 `MDEditor.Markdown` + `rehype-sanitize` 적용
- 기존 프로젝트에서 사용 중인 `@uiw/react-md-editor` 패턴 재사용 (AiFirstAnswerSection 참조)

## 작업03 — 빌드/린트 검증 + agent-browser 재현 확인

- [x] 완료

### 상세

- `pnpm build` / `pnpm lint` 통과 확인
- agent-browser로 두 버그 수정 확인

## 작업04 — 위젯 레이아웃 오버플로 수정 + 페이지 이동 시 위젯 닫기

- [x] 완료

### 상세

- `ChatbotWidget.tsx`: `overflow-hidden` → `overflow-clip` + `max-h` 추가 (scrollIntoView가 위젯 자체를 스크롤하는 문제)
- `QnaChatView.tsx` / `CsChatView.tsx`: 메시지 영역 부모에 `flex flex-col overflow-hidden` 추가 (MessageList 스크롤 정상화)
- `ChatbotPageContextSync.tsx`: pathname 변경 시 `close()` 호출 추가

## 작업05 — 빌드/린트 + agent-browser 최종 검증

- [x] 완료
