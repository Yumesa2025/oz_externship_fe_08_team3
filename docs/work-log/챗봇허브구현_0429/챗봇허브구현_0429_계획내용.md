# 챗봇허브구현*0429*계획내용

## 작업 계획

- 전체 목표: CS/Q&A를 통합해 보여주는 챗봇 허브 화면을 구현하고, 각 채팅 뷰로의 진입점을 제공한다.
- 진입 정책: FAB 클릭 → CS 직행 (기존 유지). 허브는 CS/Q&A 뒤로가기로만 진입.

## 작업01 — sessions 모듈 채우기 (데이터 레이어)

- [x] 완료
- `src/features/chatbot/sessions/` 빈 파일 4개를 채움 (types, queries, handler, index)
- `src/mocks/handlers.ts`에 sessionsHandlers 등록
- API 필드명은 FEATURES.md 기준, 실제 백엔드와 다를 수 있음 → TODO 주석

## 작업02 — HubView 컴포넌트 생성

- [x] 완료
- `src/features/chatbot/hub/HubView.tsx` 신규 생성
- `src/features/chatbot/hub/index.ts` 신규 생성
- 공통 ChatbotHeader 사용 (title="AI OZ 시스템 챗봇", showBack=false, X→close())
- CS 항목 항상 표시 (Q&A 로딩/에러와 무관하게 클릭 가능)
- Q&A 섹션 내부에서만 로딩/에러/빈 상태/재시도 처리
- Q&A 클릭 시 enterQna() 호출 (store 시그니처에 맞춤, view 전환 포함됨)
- Store 기본 view는 변경하지 않음 (cs 유지)

## 작업03 — ChatbotWidget 연결 + 빌드/린트 검증

- [x] 완료
- `ChatbotWidget.tsx`에서 로컬 HubView 제거, import 교체
- hub case의 ChatbotHeader 제거 (HubView 내부에서 직접 렌더링)
- `pnpm build` / `pnpm lint` 통과 확인
