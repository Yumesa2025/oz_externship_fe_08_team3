# 답변수정구현*0423*계획내용

## 작업 계획

- 전체 목표: QnaDetailPage에서 [답변하기] 버튼을 [답변 수정하기] 버튼으로 대체하고, 마크다운 에디터 기반 답변 수정 기능을 구현한다 (REQ-QNA-006)

---

## 전제 조건

- **REQ-QNA-005 완료 후 진행** — MarkdownEditor 공통 컴포넌트, answers feature(POST), presigned-url feature가 이미 구현되어 있어야 한다.

---

## 배경 및 제약

| 항목                | 내용                                                                                        |
| ------------------- | ------------------------------------------------------------------------------------------- |
| 구현 범위           | 답변 수정 기능 (등록과 동일 위치, 버튼 대체)                                                |
| 진입 방식           | 상세 페이지 내 [답변하기] → [답변 수정하기] 버튼으로 대체. 동일한 인라인 에디터 영역 재사용 |
| 권한                | 로그인 유저 중 본인이 작성한 답변만 수정 가능 (userId === author_id)                        |
| 자동 저장           | localStorage 임시 저장 포함 (추후 팀 결정에 따라 제거/유지)                                 |
| 마크다운 라이브러리 | REQ-QNA-005에서 설치된 `@uiw/react-md-editor` 재사용                                        |

---

## API 명세

### 기존 답변 데이터 조회

- **Method / Endpoint:** `GET /api/v1/qna/questions/{question_id}/answers`
- **호출 시점:** 수정 모드 진입 시 (에디터 초기값 바인딩 목적)
- **Response:** 답변 목록 → 목록에서 현재 수정 대상 answerId에 해당하는 항목의 `content`, `img_urls` 추출

### 답변 수정

- **Method / Endpoint:** `PUT /api/v1/qna/answers/{answer_id}`
- **Request Header:** `Authorization: Bearer {token}`, `Content-Type: application/json`
- **Request Body:**
  ```json
  { "content": "str", "img_urls": ["str"] }
  ```
- **Response 200:**
  ```json
  { "answer_id": bigint, "updated_at": "str" }
  ```
- **Error 응답:**
  ```json
  {
    "400": { "error_detail": "유효하지 않은 답변 수정 요청입니다." },
    "401": { "error_detail": "로그인한 사용자만 답변을 수정할 수 있습니다." },
    "403": { "error_detail": "본인이 작성한 답변만 수정할 수 있습니다." },
    "404": { "error_detail": "해당 답변을 찾을 수 없습니다." }
  }
  ```

### 이미지 업로드 Presigned URL

- **REQ-QNA-005와 동일** — `PUT /api/v1/qna/answers/presigned-url` (이미 구현됨)

---

## 작업01 — answers feature GET 추가 + answer-edit feature 신규 구현

> REQ-QNA-005에서 answers feature(POST)가 이미 구현되어 있으므로, GET 훅을 추가하고 수정용 feature를 별도 생성한다.

- [x] `src/features/qna/answers/types.ts` — `GetAnswersResponse` 타입 추가 (답변 목록 응답)
- [x] `src/features/qna/answers/queries.ts` — `useGetAnswers(questionId)` useQuery 훅 추가
- [x] `src/features/qna/answers/handler.ts` — `GET /api/v1/qna/questions/{question_id}/answers` MSW 핸들러 추가
- [x] `src/features/qna/answer-edit/types.ts` — `PutAnswerRequest`, `PutAnswerResponse` 타입 정의
- [x] `src/features/qna/answer-edit/queries.ts` — `usePutAnswer(answerId)` useMutation 훅
- [x] `src/features/qna/answer-edit/handler.ts` — `PUT /api/v1/qna/answers/{answer_id}` MSW 핸들러
- [x] `src/features/qna/answer-edit/index.ts` — barrel export
- [x] 완료

---

## 작업02 — AnswerForm 컴포넌트 수정 모드 확장 + localStorage 자동 저장

> REQ-QNA-005에서 등록용으로 구현된 AnswerForm 컴포넌트에 수정 모드를 추가한다.

- [x] `src/components/qna/AnswerForm/AnswerForm.tsx` 수정 모드 props 추가
  - `mode: 'create' | 'edit'` prop
  - `initialContent?: string` prop — 기존 content 에디터 초기값 바인딩
  - `initialImgUrls?: string[]` prop — 기존 img_urls 상태 초기화
  - `answerId?: number` prop — PUT API 호출 시 사용
- [x] localStorage 자동 저장 로직 추가
  - 에디터 내용 변경 시 `debounce` 적용 후 `localStorage.setItem('answer-draft-{answerId}', content)` 저장
  - 수정 모드 진입 시 localStorage 임시 저장 내용 감지 → 복원 여부 안내 (선택지 제공)
  - 수정 완료(성공 응답) 시 localStorage 임시 저장 내용 삭제
- [x] PUT API 중복 호출 방지 — 전송 중 플래그 활성화(isPending 상태) 기반으로 버튼 비활성화
- [x] beforeunload 이탈 방지 (작성 내용이 서버에 반영되지 않은 상태)
- [x] 완료

---

## 작업03 — QnaDetailPage 통합 및 에러 처리

> 상세 페이지에서 로그인 유저 ID와 author_id 비교 후 버튼 분기 처리.

- [x] `src/pages/qna/QnaDetailPage.tsx` 수정 모드 통합
  - `useGetAnswers` 호출로 기존 답변 데이터 조회
  - 현재 로그인 유저 ID(`authStore`) === 답변 `author_id` 비교
    - 일치 → [답변 수정하기] 버튼 + 수정 모드 AnswerForm 렌더링
    - 불일치 + 아직 미등록 → [답변하기] 버튼 + 등록 모드 AnswerForm 렌더링
  - `usePutAnswer` mutation 연결
  - 수정 성공 시:
    - localStorage 임시 저장 삭제
    - "모든 변경 사항이 저장되었습니다." 토스트 노출
    - 에디터 닫기 + 상세 페이지 유지 (updated_at 반영)
  - 에러 처리:
    - 400 → "유효하지 않은 답변 수정 요청입니다." 토스트 + 에디터 유지
    - 401 → "로그인한 사용자만 답변을 수정할 수 있습니다." 토스트 + 로그인 이동
    - 403 → "본인이 작성한 답변만 수정할 수 있습니다." 토스트 + 뒤로가기
    - 404 → "해당 답변을 찾을 수 없습니다." 토스트 + 목록 이동
- [x] 완료

---

## 작업04 — RBAC 권한 확인

> REQ: "수강생, 조교, 러닝 코치, 운영매니저, 어드민 권한 확인"
> 현재: isAuthenticated만 체크, role 무관하게 버튼 노출

- [ ] authStore에 허용 role 상수 정의 (`'user' | 'student' | 'admin'`)
- [ ] QnaDetailPage에서 `user.role`이 허용 role인 경우에만 답변 버튼 영역 렌더링
- [ ] 완료

---

## 작업05 — 400 에러 시 에디터 포커스

> REQ: "유효하지 않은 답변 수정 요청입니다." 토스트 노출 + 포커스
> 현재: 토스트만 노출, 에디터 포커스 없음

- [ ] MarkdownEditor에 `useImperativeHandle`로 `focus()` 메서드 외부 노출
- [ ] AnswerForm에서 editor ref를 받아 `focusEditor()` 메서드를 `useImperativeHandle`로 외부 노출
- [ ] QnaDetailPage의 400 에러 핸들러에서 `answerFormRef.current?.focusEditor()` 호출
- [ ] 완료

---

## 작업06 — 질문 요약 정보 표시

> REQ: "현재 수정 중인 답변이 달린 원본 질문의 제목 및 정보 표시 (필수)"
> 현재: "질문 내용을 불러오는 중..." 고정 placeholder

- [ ] `src/features/qna/question-detail/types.ts` — `GetQuestionDetailResponse` 타입 정의 (swagger `QuestionListDetail` 기반)
- [ ] `src/features/qna/question-detail/queries.ts` — `useGetQuestionDetail(questionId)` useQuery 훅 구현
- [ ] `src/features/qna/question-detail/handler.ts` — `GET /api/v1/qna/questions/:question_id/` MSW 핸들러 구현
- [ ] `src/features/qna/question-detail/index.ts` — barrel export
- [ ] `src/mocks/handlers.ts` — questionDetailHandler 등록
- [ ] QnaDetailPage에서 질문 제목을 AnswerForm의 `questionTitle`에 연동
- [ ] 완료

---

## 작업07 — 수정 후 updated_at 화면 반영

> REQ: "수정 시간이 반영된 상세 페이지에서 updated_at 시간과 변경된 내용이 정상적으로 보이는가?"
> 현재: PUT 성공 후 캐시 invalidate만 수행, 화면에 updated_at 미표시

- [ ] QnaDetailPage 내 답변 목록 표시 UI 구현 (answers 데이터 기반)
  - 답변 content 마크다운 렌더링 (`MDEditor.Markdown`)
  - 작성자 닉네임 표시
  - updated_at 포맷 표시
- [ ] PUT 성공 후 `useGetAnswers` 캐시 invalidate로 최신 updated_at 자동 반영 확인
- [ ] 완료
