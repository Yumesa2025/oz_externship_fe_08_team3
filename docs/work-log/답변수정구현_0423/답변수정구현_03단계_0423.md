# 답변수정구현\_03단계\_0423

## 단계 요약

- QnaDetailPage에서 useGetAnswers로 답변 조회 후 author.id === user.id 비교, 수정/등록 모드 분기 및 에러 처리 완료

## 생성 파일

- 없음

## 수정 내역

- `src/pages/qna/QnaDetailPage.tsx`
  - `useGetAnswers(numericQuestionId)` 호출 추가
  - `usePutAnswer(myAnswer?.id, numericQuestionId)` 연결
  - `answers.find(a => user?.id != null && a.author.id === user.id)`로 내 답변 탐색
  - 버튼: `isEdit` 여부에 따라 "답변 수정하기" / "답변하기" 분기
  - 수정 모드 AnswerForm에 `mode="edit"`, `initialContent`, `initialImgUrls`, `answerId` 전달
  - 수정 성공: `localStorage.removeItem('answer-draft-{id}')` + 토스트 + 에디터 닫기
  - 에러 처리: 400/401/403/404 각각 토스트 메시지 + 라우팅 처리

## 막혔던 것

- 없음

## 다음 단계 참고

- authStore.user.id가 실제 설정되어야 수정 모드 분기 동작. MSW mock에서 author.id: 211로 고정되어 있으므로 로그인 시 id: 211을 함께 저장해야 테스트 가능

## 커밋

- 미커밋
