# 답변수정구현\_02단계\_0423

## 단계 요약

- AnswerForm 컴포넌트에 수정 모드(mode='edit') 확장 및 localStorage 임시 저장/복원/삭제 로직 구현

## 생성 파일

- 없음

## 수정 내역

- `src/components/qna/AnswerForm/AnswerForm.tsx`
  - `mode`, `initialContent`, `initialImgUrls`, `answerId` prop 추가
  - 수정 모드 진입 시 localStorage 임시 저장 감지 → 복원/버리기 안내 배너 표시
  - debounce(500ms) 적용 후 `localStorage.setItem('answer-draft-{answerId}', content)` 자동 저장
  - beforeunload 이탈 방지 (기존 로직 유지)
  - 버튼 라벨 분기: 수정 모드 → "수정하기", 등록 모드 → "등록하기"
  - `isLoading`(isPending) 기반 버튼 비활성화로 중복 호출 방지

## 막혔던 것

- 없음

## 다음 단계 참고

- localStorage 임시 저장 삭제(`localStorage.removeItem`)는 QnaDetailPage에서 수정 성공 콜백에서 처리해야 함 (AnswerForm 외부에서 draftKey 알아야 하므로 `answerId` 전달 필요)

## 커밋

- 미커밋
