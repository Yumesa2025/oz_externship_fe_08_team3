# 챗봇UI수정\_02단계\_0430

## 단계 요약

- ChatbotHeader 컴포넌트를 Figma 디자인 기준으로 전면 수정

## 생성 파일

- 없음

## 수정 내역

- `src/components/chatbot/ChatbotHeader/ChatbotHeader.tsx`
  - 배경: 흰색 + `border-b` → 진보라 `#58249D`
  - 패딩: `py-3` → `py-4`
  - 타이틀: `text-sm font-semibold` 가운데 정렬 → `text-[20px] font-bold text-white` 좌측 정렬
  - `subtitle` prop 추가 (기본: "채팅의 대화 기록은 30일 동안 유지됩니다.")
  - 버튼 색상: `text-text-body hover:bg-bg-muted` → `text-white hover:opacity-80`
  - 레이아웃: `<div className="w-8">` 래퍼 → `shrink-0` 직접 방식으로 변경

## 막혔던 것

- 없음

## 다음 단계 참고

- CsChatView, QnaChatView는 ChatbotHeader를 호출만 하므로 수정 불필요 (subtitle 기본값 자동 적용)

## 커밋

- 미커밋
