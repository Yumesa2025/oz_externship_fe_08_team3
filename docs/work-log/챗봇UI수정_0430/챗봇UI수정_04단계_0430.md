# 챗봇UI수정\_04단계\_0430

## 단계 요약

- ChatInput 컴포넌트 구조 및 스타일을 Figma 디자인 기준으로 전면 수정

## 생성 파일

- 없음

## 수정 내역

- `src/components/chatbot/ChatInput/ChatInput.tsx`
  - 컨테이너: 흰 배경 + `border-gray-200` → `#F2F2F7` 배경 + `border-[#CECECE]`
  - 구조: textarea + 옆 전송 버튼 → 흰 박스 안 textarea + 하단 우측 카운터/전송
  - placeholder: `'메시지를 입력하세요'` → `'더 궁금한 것이 있다면 이어서 질문해 보세요.'`
  - 글자 카운터: 없음 → `0/1000` (현재값 보라, /1000 회색)
  - `maxLength` prop 추가 (기본 1000, 자동 슬라이스)
  - 전송 아이콘: 종이비행기 SVG + `bg-primary` 박스 → 오른쪽 화살표 `#6201E0` 아이콘만
  - rows: `1` → `2`

## 막혔던 것

- 없음

## 다음 단계 참고

- ChatInput의 props 인터페이스에 `maxLength`가 추가되었으나, 기존 호출부에서는 기본값 사용이므로 수정 불필요

## 커밋

- 미커밋
