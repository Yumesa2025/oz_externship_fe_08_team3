# 챗봇UI수정\_03단계\_0430

## 단계 요약

- ChatbotWidget 컨테이너 사이즈, 위치, radius, 그림자를 Figma 기준으로 수정

## 생성 파일

- 없음

## 수정 내역

- `src/components/chatbot/ChatbotWidget/ChatbotWidget.tsx`
  - 너비: `w-96` (384px) → `w-[360px]`
  - 높이: `h-[600px]` → `h-[608px]`
  - 하단 위치: `bottom-24` (96px) → `bottom-[120px]` (FAB 78px + 여백)
  - max-height: `calc(100vh-7rem)` → `calc(100vh-9rem)`
  - radius: `rounded-2xl` → `rounded-xl` (12px)
  - overflow: `overflow-clip` → `overflow-hidden`
  - 그림자: `shadow-xl` → inline `boxShadow: '0px 25px 50px -12px rgba(0,0,0,0.25)'`

## 막혔던 것

- 없음

## 다음 단계 참고

- 위젯 크기 변경으로 내부 ChatInput, MessageList의 레이아웃에 영향 없음 (flex 기반)

## 커밋

- 미커밋
