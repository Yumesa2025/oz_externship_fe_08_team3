# 답변댓글작성*0428*계획내용

## 작업 계획

- 전체 목표: REQ-QNA-008 기획 사양서 기반으로 QnA 상세 페이지 각 답변 하단에 댓글 작성/조회 기능을 구현한다.

---

## 작업01 — Feature 모듈 구현 (`answer-comments`)

현재 `src/features/qna/answer-comments/` 폴더는 존재하지만 모든 파일이 비어 있음.

- [x] `types.ts` — 댓글 타입 정의
  - `PostAnswerCommentRequest`: `{ content: string }`
  - `PostAnswerCommentResponse`: `{ comment_id, answer_id, author_id, created_at }`
  - `AnswerCommentItem`: 댓글 리스트 렌더링용 타입 (author, content, created_at 등)
- [x] `queries.ts` — `usePostAnswerComment(answerId)` useMutation 훅
- [x] `handler.ts` — MSW POST 핸들러 (`/api/v1/qna/answers/:answerId/comments`)
- [x] `index.ts` — barrel export
- [x] `src/features/qna/answers/types.ts`의 `comments: unknown[]`을 `AnswerCommentItem[]`으로 교체

- [x] 완료

---

## 작업02 — 댓글 UI 컴포넌트 구현 (`AnswerCommentSection`)

`src/components/qna/AnswerCommentSection/` 신규 생성.

- [x] `AnswerCommentSection.tsx`
  - 댓글 리스트 (작성자명, 작성시간, 내용)
  - 정렬 필터 (최신순 / 등록순) — 선택된 필터 색상 변경
  - 댓글 수 표기 (`댓글 N개`)
  - 댓글 입력 Textarea (maxLength=500, 실시간 글자 수 표시 `0/500`)
  - 등록 버튼 (로딩 중 비활성화, trim 공백 검사)
  - 권한 없음(비로그인) 처리: 입력창 클릭 시 "로그인 후 이용 가능합니다" 모달 노출 + 로그인 페이지 유도
- [x] `index.ts` — barrel export
- [x] `src/components/qna/index.ts`에 export 추가

- [x] 완료

---

## 작업03 — QnaDetailPage에 댓글 섹션 통합

`src/pages/qna/QnaDetailPage.tsx` 수정.

- [x] 각 답변 카드(`sortedAnswers.map`) 하단에 `<AnswerCommentSection>` 삽입
  - answerId, 초기 댓글 데이터(answer.comments), 권한(isAuthenticated) 전달
- [x] 댓글 등록 성공 시 `useGetAnswers` 캐시 무효화(invalidate) 또는 낙관적 업데이트로 리스트 즉시 갱신

- [x] 완료

---

## 작업04 — 예외 처리 및 토스트 연동

- [x] 미입력/공백: "내용을 입력해 주세요" 토스트 + 입력창 포커스 이동
- [x] 500자 초과: maxLength로 물리 차단 + 카운터 붉은색 강조 + "최대 500자까지 입력 가능합니다" 토스트
- [x] API 실패(네트워크 등): "댓글 등록에 실패했습니다. 다시 시도해 주세요." 토스트
- [x] 권한 없음(401/403): "로그인 후 이용 가능합니다" 모달 노출 + 로그인 페이지 유도
- [x] 404: "해당 답변을 찾을 수 없습니다" 토스트 + 리스트 새로고침

- [x] 완료
