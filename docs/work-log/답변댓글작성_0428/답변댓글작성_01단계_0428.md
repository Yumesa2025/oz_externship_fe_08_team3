# 답변댓글작성\_01단계\_0428

## 단계 요약

- `src/features/qna/answer-comments/` Feature 모듈 4개 파일 구현 및 MSW 핸들러 등록

## 생성 파일

- `src/features/qna/answer-comments/types.ts` — PostAnswerCommentRequest/Response, CommentAuthor, AnswerCommentItem 타입 정의
- `src/features/qna/answer-comments/queries.ts` — usePostAnswerComment(answerId, questionId) useMutation 훅
- `src/features/qna/answer-comments/handler.ts` — MSW POST 핸들러 (`/api/v1/qna/answers/:answerId/comments`), 인메모리 댓글 store
- `src/features/qna/answer-comments/index.ts` — barrel export

## 수정 내역

- `src/features/qna/answers/types.ts`: `comments: unknown[]` → `comments: AnswerCommentItem[]` 교체
- `src/mocks/handlers.ts`: `answerCommentHandlers` 등록 추가

## 막혔던 것

- R-07 위반: handler.ts의 `!content.trim() || content.length > 500` 복합 조건이 명명되지 않음 → `isInvalidContent` 변수로 추출하여 해결

## 다음 단계 참고

- `AnswerCommentItem` 타입이 정의되었으므로 작업02에서 컴포넌트 props 타입으로 바로 사용 가능
- `usePostAnswerComment(answerId, questionId)` 훅을 AnswerCommentSection 컴포넌트에서 호출

## 커밋

- 미커밋
