# 봇공통기반*0429*계획내용

## 작업 계획

- 전체 목표: 챗봇 FAB을 오버레이 위젯 방식으로 전환하고, 공통 컴포넌트(Header/MessageList/ChatInput/Widget)와 상태 구조(chatbotStore)를 구축한다. CS/QnA 전용 로직은 스코프 밖이며 공통 기반만 만든다.

## 작업01 — Phase 1: 기반 (타입 + Store + 유틸)

- [x] 완료

### 상세

- `src/features/chatbot/widgetTypes.ts` 생성: ChatbotView, ChatMessage, ChatInputDisabledReason
- `src/stores/chatbotStore.ts` 생성: authStore 패턴 (devtools 미들웨어)
  - 상태: isOpen, currentView, activeQnaQuestionId, currentPageQuestionId, firstAnswerFromProps, questionTitle
  - 액션: open(), close(), toggle(), setView(), setCurrentPageQuestionId(), enterQna()
  - close() 시 currentPageQuestionId는 유지
- `src/features/chatbot/hooks/useSSEAbort.ts` 생성: AbortController 기반 유틸 훅
  - reset(), abort(), signal, unmount 시 자동 cleanup

## 작업02 — Phase 2: 프레젠테이셔널 컴포넌트

- [x] 완료

### 상세

- `src/components/chatbot/MessageList/MessageList.tsx` + `index.ts` 생성
  - Props: { messages: ChatMessage[] }
  - user 메시지 우측(bg-primary-50), assistant 메시지 좌측(bg-bg-muted)
  - renderAssistantContent 함수 분리 (plain text, TODO: react-markdown)
  - useRef + useEffect 자동 스크롤
- `src/components/chatbot/ChatInput/ChatInput.tsx` + `index.ts` 생성
  - Props: onSend, disabled?, disabledReason?, placeholder?, notice?
  - Enter / 버튼 전송, 전송 후 초기화, disabled 처리, notice 표시

## 작업03 — Phase 3: Store 의존 컴포넌트

- [x] 완료

### 상세

- `src/components/chatbot/ChatbotHeader/ChatbotHeader.tsx` + `index.ts` 생성
  - Props: title, showBack?, onBack?, onClose?
  - onBack 기본: setView('hub'), onClose 기본: close()
  - 뒤로가기(좌, showBack=true), X 닫기(우, 항상), 타이틀 중앙
- `src/components/chatbot/ChatbotWidget/ChatbotWidget.tsx` + `index.ts` 생성
  - fixed right-6 bottom-24 z-50, w-96 h-[600px]
  - isOpen=false → null
  - currentView별 전환: hub / cs / qna (placeholder 데이터)
  - Hub: CS default item + QnA placeholder item
    - CS default item 클릭 → setView('cs')
    - QnA placeholder item 클릭 → enterQna({ questionId: 42, questionTitle: 'Q&A 챗봇', firstAnswer: null })
- `src/features/chatbot/ChatbotPageContextSync.tsx` 생성
  - useLocation + pathname 파싱: /^\/qna\/(\d+)$/ 매칭
  - setCurrentPageQuestionId(questionId | null)

## 작업04 — Phase 4: 기존 파일 수정 + 빌드 검증

- [x] 완료

### 상세

- `src/components/chatbot/ChatbotFab.tsx` 수정
  - useNavigate/ROUTES 제거, useChatbotStore/useAuthStore 추가
  - !isAuthenticated → null, 토글: isOpen ? close() : open()
  - 아이콘 전환: closed→챗봇 SVG, opened→X 아이콘
- `src/components/chatbot/index.ts` 수정: ChatbotWidget, ChatbotHeader, MessageList, ChatInput export 추가
- `src/components/layout/DefaultLayout.tsx` 수정: ChatbotWidget, ChatbotPageContextSync 추가
- 검증: `pnpm build` + `pnpm lint` 통과 확인

## 스코프 제외

- CS GET 94 / POST 93 실제 API 연동
- QnA GET 92 / POST 91 실제 API 연동
- Hub GET 95 실제 API 연동
- 실제 markdown renderer 적용
- 429 limit 실제 처리
