# 헤더통일\_01단계\_0430

## 단계 요약

- Hub/CS/QnA 헤더 레이아웃을 완전히 동일하게 통일

## 생성 파일

- 없음

## 수정 내역

- `src/components/chatbot/ChatbotHeader/ChatbotHeader.tsx`
  - subtitle이 빈 문자열일 때도 `<p>` 태그에 `\u00A0` (non-breaking space) 삽입하여 높이 유지
  - subtitle 조건부 렌더링(`{subtitle && ...}`) → 항상 렌더링으로 변경
  - `min-h-[73px]` 제거 (subtitle 영역이 항상 존재하므로 자연스럽게 높이 동일)

## 막혔던 것

- 처음 `min-h`로 높이만 맞췄으나, 타이틀/X 위치가 달랐음 → subtitle 영역 유지 방식으로 해결

## 다음 단계 참고

- 없음

## 커밋

- 미커밋
