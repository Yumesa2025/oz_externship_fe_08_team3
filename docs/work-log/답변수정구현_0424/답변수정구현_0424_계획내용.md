# 답변수정구현*0424*계획내용

## 작업 계획

- 전체 목표: REQ-QNA-006 기획 사양서 대비 미구현 기능 4가지를 모두 구현한다

## 배경

- 앞서 답변수정구현\_0423에서 작업01~03 완료 (answers GET/PUT feature, AnswerForm 수정모드, QnaDetailPage 통합)
- 기획 사양서 검토 결과 미구현 항목 4가지 추가 발견
- MarkdownEditor에 드래그 앤 드롭은 이미 구현되어 있어 제외

---

## 작업01 — RBAC 권한 확인

> REQ: "수강생, 조교, 러닝 코치, 운영매니저, 어드민 권한 확인"
> 현재: isAuthenticated만 체크, role 무관하게 버튼 노출

- [x] authStore에 허용 role 상수 정의 (`'user' | 'student' | 'admin'`)
- [x] QnaDetailPage에서 `user.role`이 허용 role인 경우에만 답변 버튼 영역 렌더링
- [x] 완료

---

## 작업02 — 400 에러 시 에디터 포커스

> REQ: "유효하지 않은 답변 수정 요청입니다." 토스트 노출 + 포커스
> 현재: 토스트만 노출, 에디터 포커스 없음

- [x] MarkdownEditor에 `useImperativeHandle`로 `focus()` 메서드 외부 노출
- [x] AnswerForm에서 editor ref를 받아 `focusEditor()` 메서드를 `useImperativeHandle`로 외부 노출
- [x] QnaDetailPage의 400 에러 핸들러에서 `answerFormRef.current?.focusEditor()` 호출
- [x] 완료

---

## 작업03 — 질문 요약 정보 표시

> REQ: "현재 수정 중인 답변이 달린 원본 질문의 제목 및 정보 표시 (필수)"
> 현재: "질문 내용을 불러오는 중..." 고정 placeholder

- [x] `src/features/qna/question-detail/types.ts` — `GetQuestionDetailResponse` 타입 정의 (swagger `QuestionListDetail` 기반)
- [x] `src/features/qna/question-detail/queries.ts` — `useGetQuestionDetail(questionId)` useQuery 훅 구현
- [x] `src/features/qna/question-detail/handler.ts` — `GET /api/v1/qna/questions/:question_id/` MSW 핸들러 구현
- [x] `src/features/qna/question-detail/index.ts` — barrel export
- [x] `src/mocks/handlers.ts` — questionDetailHandler 등록
- [x] QnaDetailPage에서 질문 제목을 AnswerForm의 `questionTitle`에 연동
- [x] 완료

---

## 작업04 — 수정 후 updated_at 화면 반영

> REQ: "수정 시간이 반영된 상세 페이지에서 updated_at 시간과 변경된 내용이 정상적으로 보이는가?"
> 현재: PUT 성공 후 캐시 invalidate만 수행, 화면에 updated_at 미표시

- [x] QnaDetailPage 내 답변 목록 표시 UI 구현 (answers 데이터 기반)
  - 답변 content 마크다운 렌더링 (`MDEditor.Markdown`)
  - 작성자 닉네임 표시
  - updated_at 포맷 표시
- [x] PUT 성공 후 `useGetAnswers` 캐시 invalidate로 최신 updated_at 자동 반영 확인
- [x] 완료
