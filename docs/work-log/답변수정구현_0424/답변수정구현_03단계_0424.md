# 답변수정구현\_03단계\_0424

## 단계 요약

- question-detail feature 전체 구현 (types/queries/handler/index), mocks 등록, QnaDetailPage에서 실제 질문 제목 연동 완료

## 생성 파일

- `src/features/qna/question-detail/types.ts` — `QuestionAuthor`, `QuestionImage`, `QuestionCategory`, `GetQuestionDetailResponse` (swagger QuestionListDetail 기반)
- `src/features/qna/question-detail/queries.ts` — `useGetQuestionDetail(questionId)` useQuery 훅
- `src/features/qna/question-detail/handler.ts` — `GET /api/v1/qna/questions/:question_id/` MSW 핸들러
- `src/features/qna/question-detail/index.ts` — barrel export

## 수정 내역

- `src/mocks/handlers.ts` — `questionDetailHandler` import 및 spread 추가
- `src/pages/qna/QnaDetailPage.tsx`
  - `useGetQuestionDetail` import 추가
  - `questionDetail` 데이터 조회 추가
  - `questionTitle` 변수 선언 (`questionDetail?.title ?? '질문 내용을 불러오는 중...'`)
  - AnswerForm의 `questionTitle` prop에 `questionTitle` 변수 연결 (TODO 주석 제거)

## 막혔던 것

- `question-detail/queries.ts`, `handler.ts`, `index.ts` 파일이 빈 파일로 존재해 Write 전 Read 필수

## 다음 단계 참고

- 마지막 단계에서 답변 목록 UI를 구현할 때 `answers` 데이터를 활용 (이미 useGetAnswers로 조회 중)

## 커밋

- 미커밋
