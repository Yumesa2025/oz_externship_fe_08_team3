# 답변수정구현\_02단계\_0424

## 단계 요약

- MarkdownEditor와 AnswerForm을 forwardRef로 전환하여 외부에서 에디터 포커스를 트리거할 수 있도록 구현, QnaDetailPage 400 에러 시 focusEditor() 호출 연결

## 생성 파일

- 없음

## 수정 내역

- `src/components/qna/MarkdownEditor/MarkdownEditor.tsx`
  - `forwardRef`로 전환, `MarkdownEditorHandle` 인터페이스 추가 (`focus()`)
  - `containerRef` 추가 후 `useImperativeHandle`로 `focus()` 구현 (내부 textarea를 querySelector로 탐색)
- `src/components/qna/AnswerForm/AnswerForm.tsx`
  - `forwardRef`로 전환, `AnswerFormHandle` 인터페이스 추가 (`focusEditor()`)
  - `editorRef(MarkdownEditorHandle)` 추가, `useImperativeHandle`로 `focusEditor()` 위임
  - `MarkdownEditor`에 `ref={editorRef}` 전달
- `src/pages/qna/QnaDetailPage.tsx`
  - `AnswerFormHandle` import 추가
  - `answerFormRef` 추가 (`useRef<AnswerFormHandle>`)
  - 등록/수정 `AnswerForm` 양쪽에 `ref={answerFormRef}` 전달
  - 400 에러 핸들러에 `answerFormRef.current?.focusEditor()` 호출 추가

## 막혔던 것

- MDEditor가 자체 textarea를 내부적으로 관리하여 직접 ref 전달 불가 → 컨테이너 div에 `containerRef`를 붙이고 `querySelector('textarea')`로 textarea를 찾아 포커스하는 방식으로 해결

## 다음 단계 참고

- 다음 단계에서 question-detail feature를 구현하고 QnaDetailPage의 `questionTitle` placeholder를 실제 데이터로 교체

## 커밋

- 미커밋
