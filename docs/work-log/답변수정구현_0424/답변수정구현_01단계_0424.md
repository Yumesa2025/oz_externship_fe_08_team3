# 답변수정구현\_01단계\_0424

## 단계 요약

- swagger RoleF6eEnum 기준으로 authStore role 타입 확장, ANSWER_ALLOWED_ROLES 상수 추가, QnaDetailPage에서 role 기반 답변 버튼 노출 제어

## 생성 파일

- 없음

## 수정 내역

- `src/stores/authStore.ts`
  - `UserRole` 타입 추가: `'USER' | 'STUDENT' | 'TA' | 'OM' | 'LC' | 'ADMIN'` (swagger RoleF6eEnum 기준)
  - `ANSWER_ALLOWED_ROLES` 상수 추가: `['STUDENT', 'TA', 'OM', 'LC', 'ADMIN']` (USER 제외)
  - `User.role` 타입을 `UserRole`로 교체
- `src/pages/qna/QnaDetailPage.tsx`
  - `ANSWER_ALLOWED_ROLES` import 추가
  - `canAnswer` 변수 추가: `isAuthenticated && user?.role != null && ANSWER_ALLOWED_ROLES.includes(user.role)`
  - 답변 섹션 렌더 조건을 `isAuthenticated` → `canAnswer`로 교체

## 막혔던 것

- 없음

## 다음 단계 참고

- 400 에러 핸들러에서 에디터 포커스 호출을 위해 AnswerForm에 ref 노출 필요

## 커밋

- 미커밋
