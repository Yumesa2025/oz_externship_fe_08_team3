# 답변수정구현\_04단계\_0424

## 단계 요약

- QnaDetailPage에 답변 목록 UI 구현 — 마크다운 렌더링, 작성자 정보, updated_at 표시. PUT 성공 후 캐시 invalidate로 최신 데이터 자동 반영

## 생성 파일

- 없음

## 수정 내역

- `src/pages/qna/QnaDetailPage.tsx`
  - `@uiw/react-md-editor`의 `Markdown` named export import 추가
  - `formatDate(iso)` 유틸 함수 추가 (ko-KR 로케일 날짜 포맷)
  - 답변 목록 UI 추가 (`answers.length > 0`일 때 렌더링):
    - 각 답변 카드: 작성자 닉네임 + 코스명/기수, `수정일: {formatted updated_at}`, 마크다운 content 렌더링
  - PUT 성공 시 `usePutAnswer` 내부의 `invalidateQueries(['answers', questionId])`가 동작하여 최신 `updated_at` 자동 반영

## 막혔던 것

- MDEditor에서 Markdown 렌더러를 `MDEditor.Markdown`이 아닌 named export `Markdown`으로 불러와야 함

## 다음 단계 참고

- 모든 미구현 기능 구현 완료. 이후 커밋 및 PR 작성 가능

## 커밋

- 미커밋
