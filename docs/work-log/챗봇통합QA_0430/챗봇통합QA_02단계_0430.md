# 챗봇통합QA_02단계\_0430

## 단계 요약

- `pnpm build` / `pnpm lint` 통과 확인 + 20개 시나리오 코드 리뷰 기반 QA 결과 체크리스트 정리

## 생성 파일

- 없음

## 수정 내역

- 없음 (검증만 수행)

## 빌드/린트 결과

- `pnpm build`: 통과 (2.81s, 경고: chunk size > 500KB — 기존 이슈, 이번 수정과 무관)
- `pnpm lint`: 통과 (에러 없음)

## QA 결과 체크리스트

### FAB/진입

- [x] 비로그인 → FAB 미표시 (`ChatbotFab.tsx:6-8` !isAuthenticated → null)
- [x] 로그인 → FAB 표시 (`ChatbotFab.tsx:6-8`)
- [x] FAB 클릭 → CS 채팅창 직행 (`chatbotStore.ts:39-40` open() → currentView: 'cs')

### CS 채팅

- [x] CS 진입 시 GET `/api/v1/chatbot/completions` 호출 (`cs/queries.ts:10-11`)
- [x] 빈 히스토리 → 환영 메시지 표시 (`useCsChat.ts:54-67` results.length === 0 → WELCOME_MESSAGE)
- [x] 메시지 전송 → POST SSE 스트리밍 (`useCsChat.ts:96-110`)
- [x] [DONE] 후 입력창 재활성화 (`useCsChat.ts:222-223` finally → setIsStreaming(false))
- [x] [DONE] 후 cache invalidation (`useCsChat.ts:224-228` CS_HISTORY_QUERY_KEY)
- [x] 뒤로가기 → abort + 허브 전환 (`CsChatView.tsx:19-22` abort() → setView('hub'))

### 허브

- [x] 허브 진입 시 GET `/api/v1/qna/ai-answer/sessions/`만 호출, CS GET 안 됨 (`HubView.tsx:8` useGetSessions만 사용)
- [x] CS 항목 항상 최상단, API 의존 없음 (`HubView.tsx:31-60` 정적 렌더링)
- [x] Q&A 세션 없음 → CS 항목만 표시, Q&A 섹션 미표시 (전체 빈 상태 아님)
- [x] 허브에서 CS 클릭 → 그때 GET `/api/v1/chatbot/completions` 호출 (CsChatView 마운트 시 useGetCsHistory)

### AI 1차 답변

- [x] 질문 상세에서 AI 답변보기 → POST 호출 (`AiFirstAnswerSection.tsx:84-94`)
- [x] AI 답변 정적 표시 + 추가 질문하기 버튼 (`AiFirstAnswerSection.tsx:106-130`)
- [x] 추가 질문하기 → enterQna({ questionId, questionTitle, firstAnswer }) (`AiFirstAnswerSection.tsx:96-103`)

### Q&A 채팅

- [x] firstAnswer가 첫 assistant 메시지로 표시 (`useQnaChat.ts:77-80` props ?? history[0])
- [x] 메시지 전송 → POST SSE 스트리밍 (`useQnaChat.ts:141-152`)
- [x] 429 → 빈 assistant 제거 + "5회 초과" 배너 + 입력 비활성 (`useQnaChat.ts:161-167`, `QnaChatView.tsx:84-88,92-94`)
- [x] 다른 questionId에서 진입 → readOnly 모드 (`useQnaChat.ts:56`)
- [x] 같은 questionId 상세로 SPA 이동 → readOnly 해제 (`ChatbotPageContextSync.tsx:12-16`)
- [x] [DONE] 후 cache invalidation (`useQnaChat.ts:275-282` QNA_HISTORY + SESSIONS)

### 공통 안전성

- [x] SSE 중 X 닫기 → abort (CS: `CsChatView.tsx:24-27`, QNA: `QnaChatView.tsx:31-34`)
- [x] SSE 중 뒤로가기 → abort (CS: `CsChatView.tsx:19-22`, QNA: `QnaChatView.tsx:26-29`)
- [x] SSE 중 페이지 이동 → abort (`useSSEAbort.ts:22-26` useEffect cleanup)

### 상태 꼬임 검증

- [x] currentView: open()→cs, setView()→hub, enterQna()→qna, close()→cs 리셋
- [x] isOpen: toggle/open/close/enterQna에서 일관성 유지
- [x] activeQnaQuestionId: enterQna에서 설정, close에서 null 리셋
- [x] currentPageQuestionId: ChatbotPageContextSync에서 pathname 변경 시 갱신, close 시 유지
- [x] qnaLimitExceededIds: Set<number>로 questionId별 독립 관리, 빈 히스토리 시 해제

## 미수정 사항 및 사유

- **unmounted setState 방어**: React 18+에서 unmounted component setState는 자동 no-op. useSSEAbort cleanup에서 abort() 호출이 추가 안전장치. isMountedRef 추가는 불필요한 복잡도이므로 이번 QA-fix 범위에서 미수정.

## 막혔던 것

- 없음

## 다음 단계 참고

- 전체 작업 완료. 커밋 대기 상태.

## 커밋

- 059a581
