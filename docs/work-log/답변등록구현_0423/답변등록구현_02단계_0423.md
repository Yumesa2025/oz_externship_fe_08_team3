# 답변등록구현\_02단계\_0423

## 단계 요약

- @uiw/react-md-editor 래핑 MarkdownEditor 공통 컴포넌트 구현 완료

## 생성 파일

- `src/components/qna/MarkdownEditor/MarkdownEditor.tsx`
- `src/components/qna/MarkdownEditor/index.ts`
- `src/components/qna/index.ts`

## 수정 내역

- 없음

## 막혔던 것

- MSW 환경에서 실제 S3 presigned_url로 PUT 업로드 불가 → fetch 에러를 try/catch로 무시하고 img_url은 정상 삽입하도록 처리

## 다음 단계 참고

- `MarkdownEditor` props: `value`, `onChange`, `imageUrls`, `onImageUrlsChange`, `error`, `height`
- 이미지 타입 검증(image/\* 아닌 경우 에러 토스트), 드래그 앤 드롭 지원 포함
- `data-color-mode="light"` 고정 (다크모드 미지원)

## 커밋

- 미커밋
