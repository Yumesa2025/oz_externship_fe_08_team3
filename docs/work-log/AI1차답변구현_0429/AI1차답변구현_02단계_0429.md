# AI1차답변구현\_02단계\_0429

## 단계 요약

- AiFirstAnswerSection 컴포넌트 생성 — 상태별 UI(idle/pending/success/error) + 에러 처리 + "추가 질문하기" 챗봇 진입

## 생성 파일

- `src/components/qna/AiFirstAnswerSection/AiFirstAnswerSection.tsx` — 핵심 컴포넌트
- `src/components/qna/AiFirstAnswerSection/index.ts` — barrel export

## 수정 내역

- `src/components/qna/index.ts` — AiFirstAnswerSection barrel export 추가

## 주요 구현 내용

- idle: RobotIcon + "AI 답변보기" 버튼 (bg-primary-50 톤)
- pending: "AI가 답변을 생성하고 있습니다..." + Spinner + 버튼 disabled
- success: MDEditor.Markdown + rehypeSanitize로 마크다운 렌더링 + "추가 질문하기" 버튼
- error: handleApiError 상태코드별 메시지 → showToast + errorMessage 로컬 상태 인라인 에러 → reset()으로 idle 복귀
- "추가 질문하기": chatbotStore.enterQna({ questionId, questionTitle, firstAnswer })
- using_model UI 미표시, SSE/타이핑 효과 없음

## 막혔던 것

- 없음

## 다음 단계 참고

- QuestionDetail.tsx에서 기존 플레이스홀더 교체 + isAuthenticated/showToast props 추가 필요

## 커밋

- 미커밋
