# 프로필드롭다운\_02단계\_0515

## 단계 요약

- ProfileDropdown에서 role === 'USER'일 때만 수강생 등록 링크를 노출하도록 조건부 렌더링 적용

## 생성 파일

- 없음

## 수정 내역

- `src/components/layout/Header/ProfileDropdown.tsx`
  - `useAuthStore` import 추가
  - `role = useAuthStore((state) => state.user?.role)` 조회 추가
  - 수강생 등록 링크를 `{role === 'USER' && (...)}` 조건부 렌더링으로 변경

## 막혔던 것

- 없음

## 다음 단계 참고

- `Header.tsx`의 `ProfileMenu`에서 onMouseLeave 이벤트 처리를 수정해 드롭다운 카드로 마우스 이동 시에도 카드가 유지되도록 수정 필요

## 커밋

- e1235c6
