# 프로필드롭다운\_01단계\_0515

## 단계 요약

- 프로필 이미지 없을 때 defaultProfile 이미지 대신 ProfileIcon SVG 컴포넌트를 사용하도록 수정

## 생성 파일

- 없음

## 수정 내역

- `src/components/layout/Header/Header.tsx`
  - `defaultProfile` import 제거
  - `ProfileIcon` import 추가 (`./icons`)
  - `ProfileMenu` 내 `<img>` 단일 렌더링 → `user.profileImage` 유무에 따라 `<img>` / `<ProfileIcon />` 조건부 렌더링으로 변경

## 막혔던 것

- 없음

## 다음 단계 참고

- `ProfileDropdown.tsx`에 `useAuthStore`에서 role을 가져와 `user.role === 'USER'`일 때만 수강생 등록 링크 노출

## 커밋

- ed044e0
