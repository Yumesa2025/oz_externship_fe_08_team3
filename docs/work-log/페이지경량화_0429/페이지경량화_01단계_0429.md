# 페이지경량화\_01단계\_0429

## 단계 요약

- src/pages/qna 하위 4개 페이지 + 관련 컴포넌트 11개 파일 분석 완료, 리팩토링 계획 수립

## 생성 파일

- 없음 (분석 단계)

## 수정 내역

- 없음 (분석 단계)

## 분석 결과

### A. 페이지 간 공통 컴포넌트 후보 (우선순위 높음)

| 항목                                   | 발견 위치                            | 분리 방향                       |
| -------------------------------------- | ------------------------------------ | ------------------------------- |
| 로딩 스피너 래퍼                       | QnaDetailPage×2, QnaListPage×2 (4회) | `LoadingBox` 공통 컴포넌트      |
| 작성자 프로필 아바타 + 이니셜 fallback | QnaDetailPage, AnswerCard (2회)      | `UserAvatar` 공통 컴포넌트      |
| 카테고리 3단 선택 상태·핸들러          | QnaWritePage, CategoryFilter (2회)   | `useCategorySelector` 커스텀 훅 |

### B. 단일 페이지 내 분리 대상 (우선순위 높음)

| 항목                                | 대상                      | 분리 방향                                             |
| ----------------------------------- | ------------------------- | ----------------------------------------------------- |
| Pagination (ChevronLeft/Right 포함) | QnaListPage.tsx:38~133    | `Pagination` 공통 컴포넌트                            |
| 질문 상세 섹션 전체                 | QnaDetailPage.tsx:304~434 | `QuestionDetail` qna 컴포넌트                         |
| 답변 목록 섹션                      | QnaDetailPage.tsx:477~531 | `AnswerSection` qna 컴포넌트                          |
| QnaWriteForm 함수                   | QnaWritePage.tsx:18~233   | `QuestionForm` qna 컴포넌트 (QnaEditPage 재사용 목적) |

### C. Tailwind 중복 패턴 (A·B 분리 시 자동 해소되는 항목 포함)

| 패턴                                         | 위치 | 처리                                  |
| -------------------------------------------- | ---- | ------------------------------------- |
| `flex justify-center py-10` + Spinner        | 4곳  | LoadingBox 흡수                       |
| `h-8 w-8 rounded-full` 아바타                | 2곳  | UserAvatar 흡수                       |
| `bg-bg-subtle ... rounded-full` 이니셜       | 2곳  | UserAvatar 흡수                       |
| Q·A 뱃지 (`bg-primary h-6 w-6 rounded-full`) | 2곳  | QaBadge 소형 컴포넌트 (중간 우선순위) |

### D. 아이콘 (우선순위 낮음)

| 아이콘              | 위치          | 처리                              |
| ------------------- | ------------- | --------------------------------- |
| ChevronLeft/Right   | QnaListPage   | Pagination 분리 시 내부 이동      |
| LinkIcon, RobotIcon | QnaDetailPage | QuestionDetail 분리 시 내부 이동  |
| ChevronDown (익명)  | QnaListPage   | Pagination·정렬 분리 시 내부 이동 |

### 변경하지 않는 것

- `QnaEditPage.tsx` — 플레이스홀더 상태
- `AnswerForm.tsx` — forwardRef·draft·beforeunload 복잡 로직
- QnaListPage URL 파라미터 동기화 로직

## 막혔던 것

- 없음

## 다음 단계 참고

- 작업02 우선순위: LoadingBox → UserAvatar → Pagination → QuestionDetail → AnswerSection → QuestionForm 순서로 진행
- useCategorySelector 훅은 CategoryFilter 파일 구조와 의존성이 있어 QuestionForm 분리 후 함께 처리

## 커밋

- 미커밋
